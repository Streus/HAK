using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// An interface for nodes that allow queries of the current time (i.e., are
/// a trusted server to check the current time).
/// </summary>
public interface ITime {
}
